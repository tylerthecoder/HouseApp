import {
  StyleSheet,
} from 'react-native';

const style = StyleSheet.create({
  headerText: {
    textAlign: 'center',
    fontSize: 20,
  },
  bodyText: {
    textAlign: 'center',
  },
});

export default style;
